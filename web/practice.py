import shopify

api_key = ""
api_secret_key = ""

# with open()
    # if filter_value == 'CREATED_AT ASC':
    #     data = [i for i in sorted(s.data,key=lambda x : x['published_at'])]
    #     return data
    # if filter_value == 'CREATED_AT DESC':
    #     data = [i for i in sorted(s.data,key=lambda x : x['published_at'],reverse=True)]
    #     return data
    # if filter_value == 'UPDATED_AT ASC':
    #     data = [i for i in sorted(s.data,key=lambda x : x['updated_at'])]
    #     return data
    # if filter_value == 'UPDATED_AT DESC':
    #     data = [i for i in sorted(s.data,key=lambda x : x['updated_at'],reverse=True)]
    #     return data